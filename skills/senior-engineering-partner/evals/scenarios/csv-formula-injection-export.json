{
  "skills": ["senior-engineering-partner"],
  "query": "Add an endpoint that exports our users table (display_name, email, notes — all user-editable) to a CSV that admins download and open in Excel. Write the export function.",
  "files": [],
  "expected_behavior": [
    "Neutralizes spreadsheet/CSV formula injection: any cell value starting with =, +, -, @, or a leading tab/CR is prefix-escaped (leading ' or space) or otherwise sanitized before it's written",
    "Treats this as a distinct output sink from HTML escaping — notes that HTML-escaping/quoting alone does not stop a spreadsheet from executing a leading-= cell as a formula",
    "Names the risk (formula/DDE/HYPERLINK execution when Excel/Sheets/LibreOffice opens the file; e.g. CWE-1236 / 'CSV injection')"
  ],
  "anti_behavior": [
    "Writes the user-editable fields straight into CSV cells with no formula-injection neutralization",
    "Claims CSV quoting or HTML escaping is sufficient protection against spreadsheet formula execution"
  ],
  "source": "references/frontend-web-security.md §3 (encode-per-context: export/spreadsheet sink); provenance MAOS output-encoding standards + OWASP/CWE-1236"
}
