---
name: tailwind-css-patterns
description: Use when writing or reviewing Tailwind CSS utility classes for common UI patterns — cards, forms, grids, navigation — configuring dark mode, or debugging Tailwind-specific issues like classes missing from the production build.
---

# Tailwind CSS Patterns

Utility-class recipes and Tailwind-specific pitfalls, framework-agnostic
(React, Vue, Svelte all apply). This is pattern reference, not aesthetic
direction — defer to whichever taste-skill is driving the build for which
direction to apply these utilities toward.

See `references/patterns.md` for the full pattern list.
